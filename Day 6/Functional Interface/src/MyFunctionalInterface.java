//can have only one undefined method(abstract method)
//if nothing in front of the method then it is by default public static
//lambda expressions can be used only with functional interfaces
public interface MyFunctionalInterface {
	
	void saySomething();
	
	
}

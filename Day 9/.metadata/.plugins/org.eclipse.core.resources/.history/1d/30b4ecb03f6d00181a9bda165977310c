
public class Driver {
	
	public static void main(String[] args) {
		//Lambda Expression
		MyFunctionalInterface obj = (a,b) -> {
			return a+" "+b;
		};
		/*MyFunctionalInterface obj = (a,b) -> System.out.println(a+b); {} is not required if we 
		are returning anything 
		-> They are state less ....means new variable cannot be declared inside lambda class
		1> Default 2>Private 3>Static are the 3 keywords that can be used with interface
		*/
		
		
		System.out.println(obj.methodA("Yamuna","K"));
		obj.methodB(0, 18);
		MyFunctionalInterface.staticMethod("Myself");
	}
}
